
// // context.beginPath();       // begin a new path
// // context.moveTo(10, 10);    // move to the starting point of the line
// // context.lineTo(100, 100);  // define the end point of the line
// // context.stroke();          // draw the line



// context.beginPath();       // begin a new path
// context.moveTo(10, 10);    // move to the starting point of the line
// context.lineTo(100, 100);  // define the end point of the line
// context.lineTo(150, 100);  // define the end point of the line
// context.lineTo(150, 100);  // define the end point of the line
// context.lineTo(250, 100);  // define the end point of the line
// context.lineTo(150, 10);  // define the end point of the line
// context.stroke();


// context.moveTo(10, 10);

// // draw the zigzag
// context.lineTo(30, 30);
// context.lineTo(50, 10);
// context.lineTo(70, 30);
// context.lineTo(90, 10);
// context.lineTo(110, 30);
// context.lineTo(130, 10);

// context.strokeStyle = 'red'; // set the stroke color
// context.stroke()